import React, { useEffect, useRef, useState } from 'react'
import _, { xor } from 'lodash'
import { usePrevious } from './usePrevious'
import e from 'cors'

const useScoreCalculator = () => {
 
}

export { useScoreCalculator }